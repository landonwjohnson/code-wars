// use keyword ```function``` to define your function, function name should be ```helloWorld```
// (don't forget the ```() and {}```) use keyword ```var``` to define a variable ```str```, 
// value of ```str``` should be a string: ```"Hello World!"```(don't forget the ```=```).
// type the ```console.log()``` in the next line(don't forget to put the ```str``` 
// in the brackets).

function helloWorld(){
    var str = "Hello World!";
    console.log(str);
  }
  